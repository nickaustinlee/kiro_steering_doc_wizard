# Development Guidelines

Generated on: {{ creation_date }}

## Project Configuration

### Testing Preferences
- **Local Testing**: {{ get_answer('local_testing', 'Not specified') }}
{%- if has_answer('use_docker') %}
- **Docker Support**: {{ get_answer('use_docker') | yesno }}
{%- endif %}
{%- if has_answer('use_pytest') %}
- **Pytest Support**: {{ get_answer('use_pytest') | yesno }}
{%- endif %}

### GitHub Configuration
{%- if has_answer('repository_url') %}
- **Repository URL**: {{ get_answer('repository_url') }}
- **GitHub Actions**: {{ get_answer('use_github_actions', false) | yesno }}
{%- else %}
- **Repository**: Not configured
{%- endif %}

### Code Formatting
- **Black Formatter**: {{ get_answer('use_black', false) | yesno }}
- **Google Style Guide**: {{ get_answer('use_google_style', false) | yesno }}

{%- if has_answer('custom_rules') %}

#### Custom Formatting Rules
```
{{ get_answer('custom_rules') }}
```
{%- endif %}

### Virtualization
- **Preference**: {{ get_answer('virtualization_preference', 'Not specified') }}
{%- if has_answer('include_venv_docs') %}
- **Include venv Documentation**: {{ get_answer('include_venv_docs') | yesno }}
{%- endif %}
{%- if has_answer('include_venv_docs_poetry') %}
- **Include venv Documentation**: {{ get_answer('include_venv_docs_poetry') | yesno }}
{%- endif %}

## Development Workflow

This document captures the development preferences for this project. These settings should be used to configure development tools and CI/CD pipelines.

### Testing Setup

{%- if is_true('use_docker') or get_answer('local_testing') == 'docker' or get_answer('local_testing') == 'both' %}

#### Docker Testing
- Use Docker containers for consistent testing environments
- Ensure Dockerfile is properly configured for the project
{%- endif %}

{%- if is_true('use_pytest') or get_answer('local_testing') == 'pytest' or get_answer('local_testing') == 'both' %}

#### Pytest Configuration
- Use pytest as the primary testing framework
- Configure pytest.ini or pyproject.toml for test discovery
- Include appropriate test coverage reporting
{%- endif %}

{%- if is_true('use_github_actions') %}

### GitHub Actions
- Configure automated testing workflows
- Repository: {{ get_answer('repository_url', 'TBD') }}
- Include testing across multiple Python versions if applicable
{%- endif %}

### Code Quality

{%- if is_true('use_black') %}

#### Black Formatter
- Use Black for automatic code formatting
- Configure line length and other Black settings as needed
{%- endif %}

{%- if is_true('use_google_style') %}

#### Google Python Style Guide
- Follow Google Python style guide for code structure
- Use appropriate docstring formats
- Maintain consistent naming conventions
{%- endif %}

{%- if get_answer('virtualization_preference') == 'poetry' or get_answer('virtualization_preference') == 'poetry_with_venv_docs' %}

### Dependency Management
- Use Poetry for dependency management and packaging
- Maintain pyproject.toml for project configuration
- Use Poetry environments for development
{%- elif get_answer('virtualization_preference') == 'venv' %}

### Virtual Environments
- Use Python venv for virtual environment management
- Maintain requirements.txt for dependencies
- Document environment setup procedures
{%- endif %}

{%- if is_true('include_venv_docs') or is_true('include_venv_docs_poetry') or get_answer('virtualization_preference') == 'poetry_with_venv_docs' %}

#### Virtual Environment Setup (Alternative)
```bash
# Create virtual environment
python -m venv venv

# Activate virtual environment
# On macOS/Linux:
source venv/bin/activate
# On Windows:
venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt
```
{%- endif %}

---

*This document was generated by the Steering Docs Wizard using questionnaire: {{ metadata.name }} v{{ metadata.version }}. Update these guidelines as your project evolves.*